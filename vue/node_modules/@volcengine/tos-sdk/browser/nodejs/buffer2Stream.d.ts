/// <reference types="node" />
import { Readable } from 'stream';
export declare class Buffer2Stream extends Readable {
    private buf;
    lastPos: number;
    constructor(buf: Buffer);
    _read(n: number): void;
}
